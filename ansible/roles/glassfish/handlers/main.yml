# roles/glassfish/handlers/main.yml
---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart glassfish
  ansible.builtin.systemd:
    name: glassfish
    state: restarted
